# Here is a random R script

x <- sample(1:100, 10, replace=FALSE)

min(x)

max(x)

range(x)

# length(x)

# something something

# more stuff
library(dplyr)
library(psrctrends)
ofm.pop <- get_ofm_intercensal_population()

